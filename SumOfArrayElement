#include <stdio.h>
int sumArray(int *arr, int size) {  
    int sum = 0;  
    for (int i = 0; i < size; i++) sum += *(arr + i);  
    return sum;  
}

int main() {
    int arr[]={3,4,5,2,5};
    int n = 5;
    printf("%d",sumArray(arr,n));
    return 0;
}
